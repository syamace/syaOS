{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Capabilities for the default window",
  "windows": ["main"],
  "remote": {
    "urls": [
      "https://sya-os.vercel.app",
      "http://localhost:*"
    ]
  },
  "permissions": [
    "core:default",
    "shell:allow-open",
    "core:window:allow-start-dragging",
    "core:window:allow-toggle-maximize"
  ]
}

